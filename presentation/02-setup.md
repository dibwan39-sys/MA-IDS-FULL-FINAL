# MA-IDS - الجزء 2: الإعداد والتشغيل
## Installation & Setup Guide

---

## 📋 متطلبات النظام

### الأجهزة المطلوبة
- **معالج**: Intel i5 أو أعلى
- **ذاكرة**: 8GB RAM على الأقل
- **مساحة القرص**: 2GB متوفرة
- **نظام التشغيل**: Windows 10/11, macOS, Linux

### البرمجيات المطلوبة
- **Node.js 18+** - وقت تشغيل JavaScript
- **npm أو yarn** - مدير الحزم
- **Python 3.8+** - للوكيل الذكي (اختياري)
- **Git** - للتحكم في الإصدارات

---

## 🚀 خطوات التثبيت

### الخطوة 1: تحميل المشروع
```bash
# استنساخ المستودع من GitHub
git clone https://github.com/YOUR_USERNAME/MA-IDS-FULL-FINAL.git
cd MA-IDS-FULL-FINAL
```

### الخطوة 2: تثبيت التبعيات
```bash
# تثبيت حزم Node.js
npm install

# أو باستخدام yarn
yarn install
```

### الخطوة 3: إعداد متغيرات البيئة
```bash
# إنشاء ملف .env
cp .env.example .env

# تحرير المتغيرات حسب الحاجة
VITE_BASE44_APP_BASE_URL=your_base44_url
```

### الخطوة 4: تشغيل التطبيق
```bash
# تشغيل خادم التطوير
npm run dev

# أو للإنتاج
npm run build
npm run preview
```

---

## 🔧 إعداد Base44 (اختياري)

### إنشاء حساب Base44
1. اذهب إلى [base44.io](https://base44.io)
2. أنشئ حساب جديد
3. أنشئ مشروع جديد

### ربط التطبيق
```javascript
// في src/api/base44Client.js
const base44Config = {
  appId: 'your_app_id',
  apiKey: 'your_api_key'
};
```

---

## 📊 فحص التشغيل

### فحص الواجهة الأمامية
- افتح المتصفح على `http://localhost:5173`
- تأكد من تحميل الصفحة الرئيسية
- فحص استجابة الأزرار والروابط

### فحص الوظائف الأساسية
- انتقل إلى لوحة التحكم
- فحص مؤشرات الأداء
- اختب عرض النشاط الأخير

### فحص الاستجابة
- غير حجم النافذة
- اختب على الأجهزة المحمولة
- فحص التنقل بين الصفحات

---

## 🐛 حل المشاكل الشائعة

### مشكلة: فشل تثبيت التبعيات
```bash
# مسح cache npm
npm cache clean --force

# إعادة التثبيت
rm -rf node_modules package-lock.json
npm install
```

### مشكلة: خطأ في التشغيل
```bash
# فحص إصدار Node.js
node --version

# فحص المنافذ المستخدمة
netstat -ano | findstr :5173
```

### مشكلة: مشاكل في التصميم
```bash
# إعادة بناء ملفات CSS
npm run build
```

---

## 📈 الأداء والتحسين

### تحسين الأداء
- استخدام lazy loading للمكونات
- تحسين الصور والأصول
- تقليل حجم الحزمة

### مراقبة الأداء
- استخدام React DevTools
- فحص استهلاك الذاكرة
- مراقبة أوقات التحميل

---

**المتابعة للجزء 3: المكونات الأساسية**